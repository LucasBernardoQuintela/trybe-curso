# Os Exercicios de hoje foram apenas de fixação dos comandos do docker.
## Os comandos utilizados foram:

```
- docker container run/create/attach
- docker pull
- parametros:
  - -ti -> para abrir o container de forma interativa
```